# [模块/关注点] 设计

（最后更新：YYYY-MM-DD）

---

## 设计背景

（一段话讲清楚：这个模块解决什么问题、在整个系统结构中的位置、服务于哪些需求。读完后应该知道"这个东西为什么存在"。）

## 系统结构

（先建地图再填细节。用缩进/编号表示分层或拓扑关系，标注每个组件的：稳定性、读写归属、一句话职责。）

```
[component_a]        ← 稳定性：几乎不变 | 写入：系统初始化 | 职责：xxx
  [sub_component]    ← 稳定性：session 级 | 写入：用户配置 | 职责：xxx
[component_b]        ← 稳定性:每轮变化 | 写入：AI 工具 | 职责：xxx
[component_c]        ← 稳定性：短命     | 写入：系统运行时 | 职责：xxx
```

结构图按稳定性从上到下排（稳定在上，频繁变化的在下），或按数据流向排。让人一眼看出"什么东西在哪里、谁控制它、它有多可靠"。

## 组件详述

（系统中每个组件展开讲。不是每个组件都需要所有字段——只写对这个组件有信息价值的。）

### [组件名]

- **稳定性**：几乎不变 / session 级 / 每轮变化 / 每轮多次 / 短命
- **写入方**：谁写这个组件（系统初始化、用户配置、AI 工具调用、系统运行时）
- **读取方**：谁读这个组件（AI 模型只读、系统内部、用户可见）
- **职责**：这个组件做什么，一句话
- **内容**：具体包含什么数据/信息，不写"包含相关配置"这种废话——写清楚是什么配置
- **与其他组件的关系**：依赖谁的数据、被谁消费、和谁有边界

### [下一个组件]

（同上，只写有信息价值的字段）

## 关键流程

（选 1-3 个关键场景，按"谁在什么时候做了什么"描述组件间协作。不是所有架构都需要这一节——只有协作关系不直观时才写。）

### 流程：[场景名]

1. [哪个组件] 做了 [什么]，触发 [什么]
2. [哪个组件] 读取 [哪个组件] 的 [什么数据]
3. ...

## 设计决策

（只放有过 tradeoff 讨论的决策——不需要覆盖所有显而易见的选择。）

### 决策：[标题]

**为了谁**：（服务于哪个需求故事或组件）

**怎么设计**：（具体到组件级别——不是"用微服务架构"这种空话）

**为什么这样**：（理由、替代方案及为何不选、取舍。如果替代方案有具体名字或出处，写出来。）

## 边界与约束

（可选。明确不做什么、不碰什么、和哪些模块有清晰边界。当模块容易被误用时最有用。）
