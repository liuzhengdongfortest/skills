# 工程治理指南

工程治理是“参谋职责”和“Roadmap 质量”在代码任务里的自然结果。自主性不只是多做功能，也包括在结构开始变坏时主动换轨。

## 复杂度换轨

出现下面任一信号时，继续堆功能通常是不负责任的；必须暂停新增功能，创建或推进工程化 Roadmap/task：

- 单文件超过 500 行，或明显承担 UI/render/API/state/runtime/test helper 任意三类职责。
- 临时代码承载超过 2 个正式产品能力。
- 调试目录、临时 state、截图、日志或一次性验证产物污染项目根目录。
- smoke/helper/mock 代码和产品 runtime 边界混杂。
- 同一个功能在多个脚本里复制粘贴，或修改一个行为必须同时改多处。
- 文档持续宣称是临时验证，但代码已经在承载正式产品功能。

换轨不是停工。正确动作是把当前功能推进暂停在安全点，记录原因，创建工程基线修复条目，然后先清理承载结构。

## 正式产品阶段

一旦老板确认项目不是 MVP、demo、POC，而是正式产品，执行标准立刻升级：

- 新增功能必须满足可维护结构、测试隔离、清理纪律和文档一致性。
- demo/prototype 代码只能放在明确的 `poc/`、`spike/` 或临时目录，并写清退出条件。
- 不能继续用调试 HTML、单文件脚本、大杂烩模块承载正式产品。
- 技术栈、设计系统、后端边界等基础选择必须进入 architecture，而不是靠当前文件惯性延续。
- 验收必须包含工程现场检查：根目录清洁、临时产物隔离、运行入口清晰。

## 每轮工程自检

Task 执行、Roadmap 推进和写代码完成前，必须快速回答：

- 这次改动是否扩大了大文件、脚本堆、全局状态、重复逻辑或临时产物？
- 测试 helper 是否混进了产品 runtime？
- 文档和代码是否继续偏离？
- 现在是否已经超过”临时实现”的阈值，需要工程化 task？
- 这次拆分/提取是否真的有认知收益，还是只是把文件切得更碎？机械提取 12 行代码到新文件不算工程治理——真正的治理降低认知负担，虚假的治理增加跳转成本。

如果答案是“是”，不能只汇报完成；要么本轮直接修，要么创建/推进对应工程化条目，并在 task 记录里写清楚。

## 新业务逻辑的放置

AI 常犯的错是为了“最小化更改”，把新增业务逻辑直接塞进当前文件。最小改动不等于最小 diff；真正的最小风险是让新逻辑有清楚边界。

新增业务逻辑前先判断信息聚合程度：

- 如果新逻辑有独立概念、独立状态、独立规则、独立测试价值，优先放到单独文件或模块。
- 原文件只做接线：调用新模块、传入依赖、处理返回值。
- 如果新增逻辑只是现有函数的一小段自然分支，才内联。
- 如果为了接入新逻辑需要在旧文件多处插入状态和分支，说明应该先抽模块。
- 如果一个文件已经很大，新增业务逻辑默认不继续往里塞，除非有明确理由。

判断口诀：

- 新概念，新文件。
- 旧流程，接线点。
- 规则内聚，模块承载。
- 最小 diff 不是目标，最小认知负担才是目标。
